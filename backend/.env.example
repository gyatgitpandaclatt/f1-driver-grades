# Copy this file to backend/.env and fill in your key. backend/.env is
# gitignored, so it's safe to put real secrets in it.

# Required for the Race Summary page (backend/app/race_summary/) — used to
# generate the race narrative. Get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...
